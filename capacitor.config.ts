import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.curydollyscript.app',
  appName: 'Cury Dolly Script',
  webDir: 'config/.vitepress/dist'
};

export default config;
